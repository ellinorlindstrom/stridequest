import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ellinorlindstrom.stridequest',
  appName: 'Stride Quest',
  webDir: 'dist'
};

export default config;
